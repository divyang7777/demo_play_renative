"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _path = _interopRequireDefault(require("path"));

var _fs = _interopRequireDefault(require("fs"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var create = function create() {
  var opts = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  return new Promise(function (resolve, reject) {
    var data = _fs.default.readFileSync(opts.source);

    _fs.default.writeFile(opts.destination, 'module.exports = `' + data.toString() + '`\n', function (err) {
      if (err) {
        reject(err);
      }

      resolve();
    });
  });
};

var createSync = function createSync() {
  var opts = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};

  var data = _fs.default.readFileSync(opts.source);

  try {
    _fs.default.writeFileSync(opts.destination, 'module.exports = `' + data.toString() + '`\n');
  } catch (e) {
    console.log('Svg2Js:Error: ', e);
  }
};

var convert = function convert() {
  var opts = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
  return new Promise(function (resolve, reject) {
    var data = _fs.default.readFileSync(opts.source);

    resolve(data.toString());
  });
};

var mkdirp = function mkdirp(dir) {
  var sep = '/';
  var segments = dir.split(sep);
  var current = '';
  var i = 0;

  while (i < segments.length) {
    current = current + sep + segments[i];

    try {
      _fs.default.statSync(current);
    } catch (e) {
      _fs.default.mkdirSync(current);
    }

    i++;
  }
};

var _default = {
  create: create,
  createSync: createSync,
  convert: convert
};
exports.default = _default;